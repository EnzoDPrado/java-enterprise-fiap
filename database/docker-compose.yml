version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: hibernate_study_postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: hibernate
    ports:
      - "5432:5432"
    volumes:
      - ./volume:/var/lib/postgresql/data
